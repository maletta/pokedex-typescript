import styled from "styled-components";

const ModalSortContainer = styled.div`
  padding: 0px 40px 50px 40px;
`;

const ButtonGroup = styled.div`
  display: grid;
  grid-template-columns: 1fr;
  grid-row-gap: 20px;

  margin-top: 35px;
`;

export { ModalSortContainer, ButtonGroup };
